import * as React from 'react'
import { Route } from 'react-router-dom'
import { Components } from './Components'

export const componentsRoutes = [
  <Route key="/components" path="/components" component={Components} />,
]
